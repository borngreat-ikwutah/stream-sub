# Client-side variables (accessible in browser, prefixed with VITE_)
VITE_API_URL=http://localhost:3001/api
VITE_APP_NAME=EasyLedger

# Server-side variables (only accessible on server)
DATABASE_URL=postgresql://postgres:password@localhost:5432/admin_dashboard
SESSION_SECRET=your-super-secret-session-key-must-be-at-least-32-characters-long

# Database configuration (used by Drizzle Kit)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=admin_dashboard

# Environment
NODE_ENV=development
